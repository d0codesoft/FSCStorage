<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:fsc.mob.client.ViewModels"
             xmlns:api="clr-namespace:fsc.mob.client.Api"
             x:Class="fsc.mob.client.Pages.MaintenancePage"
             x:DataType="viewModels:MaintenanceViewModel"
             Title="Maintenance">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="14">
            <HorizontalStackLayout Spacing="10">
                <Button Text="Refresh" Command="{Binding RefreshCommand}" />
                <Button Text="Consistency check" Command="{Binding TriggerConsistencyCheckCommand}" />
            </HorizontalStackLayout>

            <Button Text="Cleanup deleted tenants" Command="{Binding TriggerDeletedTenantCleanupCommand}" />

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
            <Label Text="{Binding StatusMessage}" TextColor="Green" />
            <Label Text="{Binding ErrorMessage}" TextColor="Crimson" />

            <Label Text="Active tasks" FontSize="18" FontAttributes="Bold" />
            <CollectionView ItemsSource="{Binding ActiveTasks}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="api:BackgroundTaskViewModel">
                        <Border Padding="12" Margin="0,0,0,8">
                            <VerticalStackLayout Spacing="4">
                                <Label Text="{Binding TypeName}" FontAttributes="Bold" />
                                <Label Text="{Binding StatusSummary}" />
                                <Label Text="{Binding TimelineLabel}" FontSize="12" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label Text="Completed tasks" FontSize="18" FontAttributes="Bold" />
            <CollectionView ItemsSource="{Binding CompletedTasks}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="api:BackgroundTaskViewModel">
                        <Border Padding="12" Margin="0,0,0,8">
                            <VerticalStackLayout Spacing="4">
                                <Label Text="{Binding TypeName}" FontAttributes="Bold" />
                                <Label Text="{Binding StatusSummary}" />
                                <Label Text="{Binding TimelineLabel}" FontSize="12" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
