// Общий модуль FscFilestorageКлиентСервер
// Клиент, Сервер, Вызов сервера
// 1С:Предприятие 8.3

#Область Константы

Функция РазмерЧастиПоУмолчанию() Экспорт
	// 10 МБ.
	Возврат 10 * 1024 * 1024;
КонецФункции

#КонецОбласти

#Область URL

Функция URLЗагрузкиФайла() Экспорт
	Возврат "/api/files";
КонецФункции

Функция URLСпискаФайлов() Экспорт
	Возврат "/api/files";
КонецФункции

Функция URLИнформацииОФайле(ФайлGuid) Экспорт
	Возврат "/api/files/" + КодироватьСтрокуДляURL(Строка(ФайлGuid)) + "/metadata";
КонецФункции

Функция URLСкачиванияФайла(ФайлGuid) Экспорт
	Возврат "/api/files/" + КодироватьСтрокуДляURL(Строка(ФайлGuid));
КонецФункции

Функция URLУдаленияФайла(ФайлGuid) Экспорт
	Возврат "/api/files/" + КодироватьСтрокуДляURL(Строка(ФайлGuid));
КонецФункции

Функция URLMultipartInit() Экспорт
	Возврат "/api/multipart/init";
КонецФункции

Функция URLMultipartPart(UploadId, НомерЧасти) Экспорт
	Возврат "/api/multipart/" + КодироватьСтрокуДляURL(Строка(UploadId))
		+ "/parts/" + Формат(НомерЧасти, "ЧГ=0");
КонецФункции

Функция URLMultipartStatus(UploadId) Экспорт
	Возврат "/api/multipart/" + КодироватьСтрокуДляURL(Строка(UploadId)) + "/status";
КонецФункции

Функция URLMultipartComplete(UploadId) Экспорт
	Возврат "/api/multipart/" + КодироватьСтрокуДляURL(Строка(UploadId)) + "/complete";
КонецФункции

Функция URLMultipartAbort(UploadId) Экспорт
	Возврат "/api/multipart/" + КодироватьСтрокуДляURL(Строка(UploadId)) + "/abort";
КонецФункции

#КонецОбласти

#Область JSON

Функция ВJSON(Данные) Экспорт
	Запись = Новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись, Данные);
	Возврат Запись.Закрыть();
КонецФункции

Функция ИзJSON(ТекстJSON) Экспорт
	Если ПустаяСтрока(ТекстJSON) Тогда
		Возврат Неопределено;
	КонецЕсли;

	Чтение = Новый ЧтениеJSON;
	Чтение.УстановитьСтроку(ТекстJSON);
	Возврат ПрочитатьJSON(Чтение);
КонецФункции

Функция РезультатHTTP(КодСостояния, Тело, Заголовки = Неопределено) Экспорт
	Результат = Новый Структура;
	Результат.Вставить("КодСостояния", КодСостояния);
	Результат.Вставить("Успешно", КодСостояния >= 200 И КодСостояния < 300);
	Результат.Вставить("Тело", Тело);
	Результат.Вставить("Заголовки", Заголовки);

	Если ЗначениеЗаполнено(Тело) Тогда
		Попытка
			Результат.Вставить("Данные", ИзJSON(Тело));
		Исключение
			Результат.Вставить("Данные", Неопределено);
		КонецПопытки;
	Иначе
		Результат.Вставить("Данные", Неопределено);
	КонецЕсли;

	Возврат Результат;
КонецФункции

#КонецОбласти

#Область Утилиты

Функция КодироватьСтрокуДляURL(Значение) Экспорт
	Возврат КодироватьСтроку(Строка(Значение), СпособКодированияСтроки.КодировкаURL);
КонецФункции

Функция ИмяФайлаИзПути(ПутьКФайлу) Экспорт
	Файл = Новый Файл(ПутьКФайлу);
	Возврат Файл.Имя;
КонецФункции

Функция РазмерФайла(ПутьКФайлу) Экспорт
	Файл = Новый Файл(ПутьКФайлу);
	Возврат Файл.Размер();
КонецФункции

Функция MIMEТипПоИмениФайла(ИмяФайла) Экспорт
	Расширение = НРег(РасширениеФайла(ИмяФайла));

	Если Расширение = ".txt" Тогда
		Возврат "text/plain";
	ИначеЕсли Расширение = ".json" Тогда
		Возврат "application/json";
	ИначеЕсли Расширение = ".pdf" Тогда
		Возврат "application/pdf";
	ИначеЕсли Расширение = ".jpg" Или Расширение = ".jpeg" Тогда
		Возврат "image/jpeg";
	ИначеЕсли Расширение = ".png" Тогда
		Возврат "image/png";
	ИначеЕсли Расширение = ".zip" Тогда
		Возврат "application/zip";
	КонецЕсли;

	Возврат "application/octet-stream";
КонецФункции

Функция РасширениеФайла(ИмяФайла) Экспорт
	Позиция = СтрДлина(ИмяФайла);
	Пока Позиция > 0 Цикл
		Символ = Сред(ИмяФайла, Позиция, 1);
		Если Символ = "." Тогда
			Возврат Сред(ИмяФайла, Позиция);
		КонецЕсли;
		Если Символ = "/" Или Символ = "\" Тогда
			Прервать;
		КонецЕсли;
		Позиция = Позиция - 1;
	КонецЦикла;

	Возврат "";
КонецФункции

Функция НовыйBoundary() Экспорт
	Возврат "----FSCStorageBoundary" + СтрЗаменить(Строка(Новый УникальныйИдентификатор), "-", "");
КонецФункции

Функция CRLF() Экспорт
	
	
	ФоновыеЗадания.Выполнить(,,,"");
	
	Возврат Символы.ВК + Символы.ПС;      
КонецФункции

#КонецОбласти